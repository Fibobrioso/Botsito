# Version y hash de la spec (F11, ADR-0013). Lo genera `botsito spec manifest`; no se edita a
# mano. El hash cubre los TRES ficheros: si quedara fuera el registro, cambiar el stop no cambiaria
# la version y el pre-vuelo de la demo daria verde con otra estrategia.
#
# Se hashea la ESTRUCTURA re-serializada, no los bytes: un comentario no cambia la spec. Por
# parametro entran nombre, categoria, tipo, unidad, estado, valor, fuente, ambiguedad_id, opciones
# y limites; el estado y la fuente cuentan, porque cambian lo que la spec afirma.
#
# Semver: mayor si una regla cambia de sentido o desaparece, menor si se anade una regla o un
# parametro, parche si solo cambia un valor o una redaccion.
spec_version: "12.2.0"
hash: 7743b9a056223b78e91e3119273a7e2be9c094b483b6d5d4d72f7b3647448ba0
generado_el: "2026-09-24T20:10:04Z"
cubre:
  - knowledge/spec/parametros.yaml
  - knowledge/spec/strategy_spec.yaml
  - knowledge/spec/glossary.yaml
