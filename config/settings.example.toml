# Copiar a config/settings.local.toml (ignorado por git). Sin secretos aqui.
# Los parametros de negocio NO viven en este fichero: viven en knowledge/spec/parametros.yaml
# (la unica puerta, ADR-0002) y se leen por botsito.config.registro.
[entorno]
nombre = "backtest"   # backtest | tester | demo

[rutas]
corpus = "corpus"
data = "data"
knowledge = "knowledge"
